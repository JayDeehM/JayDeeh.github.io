<%= form_for @skill, html: { multipart: true } do |f| %>
  <% if @skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@skill.errors.count, "error") %> prohibited this skill from being saved:</h2>

      <ul>
      <% @skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  
  <div class="field">
    <%= f.label :imgur %><br>
    <%= f.text_field :imgur, class:'enterfield' %>
  </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title, class:'enterfield' %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
